resources:
- repo: self
  fetchDepth: 1
pool:
  vmImage: windows-latest
trigger:
  branches:
    include:
    - master
  paths:
    include:
    - DbUsers
variables:
  TargetFramework: 'net5.0'
  BuildConfiguration: 'Release'
  DockerImageName: 'utility-descent-185119/webapp-repo/dbsql'
steps:
- task: MSBuild@1
  displayName: Publish
  inputs:
    solution: 'DbUsers/DbUsers.sqlproj'
- task: Docker@2
  displayName: 'Login to Container Registry'
  inputs:
    command: login
    containerRegistry: 'gar-webapp-repo'
- task: Docker@2
  displayName: 'Build and push image'
  inputs:
    Dockerfile: '$(Build.SourcesDirectory)/DbUsers/Dockerfile'
    command: buildAndPush
    repository: '$(DockerImageName)'
    buildContext: '$(Build.SourcesDirectory)'
    tags: |
      dev
